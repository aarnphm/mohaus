# {{project_name}}

Mixed Python and Mojo package scaffolded by mohaus.

```bash
nix develop
nix develop -c pre-commit run --all-files
python -c "import {{import_name}}; print({{import_name}}.passthrough('hello'))"
```

Without Nix:

```bash
uv pip install -e .
python -c "import {{import_name}}; print({{import_name}}.passthrough('hello'))"
```

Nightly Modular wheels:

```bash
uv pip install -e . --prerelease allow --extra-index-url https://whl.modular.com/nightly/simple/
```
